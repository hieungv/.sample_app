<% provide :title, t("static_pages.home.title") %>

<div class="center jumbotron">
  <h1><%= t ".head" %></h1>
  <%= link_to t("static_pages.home.sign_up"), new_path, class: "btn btn-lg btn-primary" %>
</div>
