<li>
  <%= gravatar_for user, Settings.max_length_user %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user.current_user?(user) %>
    | <%= link_to t("delete"), user, method: :delete, data: {confirm: t("mes")} %>
  <% end %>
</li>
